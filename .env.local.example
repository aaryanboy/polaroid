# URL of your deployed signaling server (see /server).
# For local dev, run the server (cd server && npm install && npm start)
# and leave this pointing at localhost.
NEXT_PUBLIC_SIGNALING_URL=http://localhost:4000

METERED_DOMAIN=same-sky.metered.live


METERED_SECRET_KEY=random key like KSDFKJSDLFSDFJKSDDBJKCSDHFGHFSD
